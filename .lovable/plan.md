## Plan: Add a README.md

Create a `README.md` at the project root describing the **AI Productivity Hub** app.

### Sections to include
1. **Project title & tagline** — AI Productivity Hub: Email Generator, Meeting Summarizer & Smart Task Planner.
2. **Overview** — what the app does in 2–3 sentences.
3. **Features**
   - Smart Email Generator (tone/language, multiple types, editable drafts)
   - Meeting Notes Summarizer (summary, decisions, action items, Markdown export)
   - AI Task Planner (daily/weekly schedule, Eisenhower matrix)
   - Dashboard, History, Analytics, Settings, Help
4. **Tech stack** — TanStack Start v1, React 19, Vite 7, Tailwind v4, shadcn/ui, Recharts, AI SDK with Lovable AI Gateway (`google/gemini-3-flash-preview`), localStorage persistence.
5. **Getting started** — `bun install`, `bun dev`, build commands.
6. **Project structure** — short tree of `src/routes`, `src/lib`, `src/components`.
7. **AI & privacy note** — Responsible AI disclaimer reference.
8. **Deployment** — Lovable preview/published URLs.
9. **License** — MIT placeholder.

### About "push to GitHub"
GitHub sync in Lovable is **automatic and bidirectional** when the repo is connected — any file I add is pushed on save. I will not run git commands manually (they're disallowed). If GitHub isn't connected yet, you'll need to connect it via the **+ menu → GitHub → Connect project** in the editor; after that, the new `README.md` syncs automatically.

### Files
- `README.md` (new)
